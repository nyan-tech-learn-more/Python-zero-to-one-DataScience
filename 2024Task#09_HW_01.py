- **Task #09 作業 01【簡答題】函式使用**
    
    繳交作業時請直接複製以下題目到作業填答區，並將「(在這裡回答)」部分改寫成你的答案，不用附上題目、標題設定成「2024 Task#09 作業 01」：
    
    1. 解釋看看你心中的「函式」是什麼？為什麼要使用「函式」？
    
    程式碼更容易管理、維護、擴展
    
    2. 試著說明以下程式的「執行順序」，執行的順序會長怎樣（盡可能地拆分為運算的最小單位）？
    
    ```python
    def f(a, b):
        if a > b:
            return a
        return b
    
    max = f(3, 5)
    max = f(6, f(3, 5))
    max = f(max, f(1, 2))
    ```
    
    (在這裡回答)
    
    ```python
    def f(a, b):
        if a > b:
            return a
        return b
    
    max = f(3, 5)#b=5 -> max=5
    max = f(6, f(3, 5)) #6,5->max =6
    max = f(max, f(1, 2)) #6,2->max=6
    ```
    
    3. 以下程式可以會傳 a, b 的最大值，如何改寫成可以接收 f(a, b), f(a, b, c), f(a, b, c, ...) 等不同個數輸入的最大值函式可以怎麼做？
    
    ```python
    def f(a, b):
        if a > b:
            return a
        return b
    
    f(3, 5) # O
    f(3, 4, 5) # X
    f(3, 4, 5, 6, 7) # X
    ```
    
    (在這裡回答)
    
    ```python
    def f(*args):
      list=[]
      for i in args:
          list.append(i)
      return max(list) #max函數回傳最大值
    
    print(f(3, 5)) # O
    f(3, 4, 5) # X
    f(3, 4, 5, 6, 7) # X
    ```
    
    4. 請問下方程式碼會回傳什麼結果？
    
    ```python
    a = 'A'
    
    def f(b):
        a = 'AAA'
        c = 'C'
        print(a)
        print(b)
        print(c)
        return b
    
    print(a)
    print(f('B'))
    print(a)
    ```
    
    (在這裡回答)
    
    A
    
    AAA
    
    B
    
    C
    
    B
    
    A
    
    - 在函式 `f` 之外，變數 `a` 被賦值為字串 "A"。
    - 當函式 `f` 被調用時，它會創建一個新的局部作用域。在這個作用域中，變數 `a` 被重新賦值為字串 "AAA"。
    - 函式 `f` 依次輸出 `a`、`b` 和 `c` 的值。
    - 函式 `f` 返回 `b`。
    - 在函式 `f` 之外，`print` 函式輸出變數 `a` 的值。
    
    **以下是程式碼的執行步驟：**
    
    **步驟 1：**
    
    變數 `a` 被賦值為字串 "A"。
    
    **步驟 2：**
    
    函式 `f` 被調用，並將字串 "B" 作為參數傳遞。
    
    **步驟 3：**
    
    函式 `f` 創建一個新的局部作用域。
    
    **步驟 4：**
    
    在函式 `f` 的局部作用域中，變數 `a` 被重新賦值為字串 "AAA"。
    
    **步驟 5：**
    
    字串 "AAA" 被輸出。
    
    **步驟 6：**
    
    字串 "B" 被輸出。
    
    **步驟 7：**
    
    字串 "C" 被輸出。
    
    **步驟 8：**
    
    字串 "B" 被返回。
    
    **步驟 9：**
    
    `print` 函式輸出變數 `a` 的值。
    
    **步驟 10：**
    
    字串 "A" 被輸出。
    
    **因此，輸出結果是：**
    
    `A
    AAA
    B
    C
    B
    A`
    
    **以下是每個階段的詳細說明：**
    
    **階段 1：**
    
    - 在這個階段，只有一個變數 `a`，它被賦值為字串 "A"。
    
    **階段 2：**
    
    - 在這個階段，函式 `f` 被調用，並將字串 "B" 作為參數傳遞。
    - 函式 `f` 創建一個新的局部作用域。在這個作用域中，變數 `a` 和 `c` 被重新定義。
    
    **階段 3：**
    
    - 在這個階段，函式 `f` 的局部作用域變為活動作用域。
    - 在這個作用域中，變數 `a` 被賦值為字串 "AAA"，變數 `c` 被賦值為字串 "C"。
    
    **階段 4：**
    
    - 在這個階段，`print` 函式輸出變數 `a` 的值。
    - 由於 `a` 在局部作用域中被重新定義，因此輸出結果為 "AAA"。
    
    **階段 5：**
    
    - 在這個階段，`print` 函式輸出參數 `b` 的值。
    - 因此，輸出結果為 "B"。
    
    **階段 6：**
    
    - 在這個階段，`print` 函式輸出變數 `c` 的值。
    - 因此，輸出結果為 "C"。
    
    **階段 7：**
    
    - 在這個階段，函式 `f` 返回 `b`。
    
    **階段 8：**
    
    - 在這個階段，函式 `f` 的局部作用域被銷毀。
    - 變數 `a` 和 `c` 恢復為其原始值。
    
    **階段 9：**
    
    - 在這個階段，`print` 函式輸出變數 `a` 的值。
    - 由於 `a` 在函式 `f` 之外被賦值為 "A"，因此輸出結果為 "A"。
    
    5. 請問下方程式碼會回傳什麼結果？

a = 'A'

def f(b):
    c = 'C'
    print(a)
    a = 'AAA'
    print(b)
    print(c)
    return b

print(a)
print(f('B'))
print(a)
